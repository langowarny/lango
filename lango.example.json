{
  "agent": {
    "provider": "google",
    "model": "gemini-2.0-flash"
  },
  "providers": {
    "google": {
      "type": "gemini",
      "apiKey": "${GOOGLE_API_KEY}"
    }
  },
  "channels": {
    "telegram": {
      "enabled": true,
      "botToken": "${TELEGRAM_BOT_TOKEN}"
    }
  }
}
